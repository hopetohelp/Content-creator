# הנחיות עבודה — Content-creator

ריפו להפקת תוכן (וידאו, אודיו, ויזואליה) בכלים מקומיים וחינמיים.
**העבודה מתבצעת בסשן ענן.** הנימוק המלא: `מסמכים/3. למה הפקה בענן.md`.

---

## 1. אילוצים קשיחים

- **תקציב $0.** אין רכישות, אין paid API, אין הרשמה לשירות.
  ✅ מותרים במפורש: `free tiers` **וגם** `free credits`.
  🔴 **הכרעה פתוחה:** `GEMINI_API_KEY` שבסביבה יושב ב-Tier 1 (חשבון חיוב מקושר) —
  תמונות, Lyria ו-Veo פעילים דרכו. **ייתכן שההוצאה יורדת מ-$300 קרדיט פתיחה
  ואז היא מותרת**; צריך לבדוק ב-Billing → Credits. Gemini TTS חינם בכל מקרה.
  עד שהבעלים בוחר מסלול (סעיף 0 ב-`מסמכים/6. הנחיות הפקה מומלצות.md`) —
  **מסלול A בלבד: TTS ותו לא.**
  🔴 **Veo אסור בלי אישור בכתב** — 90 שניות = $4.50–$54.
- 🔴 **איסור גורף על נשים — חזותית וקולית.**
  - **אין נשים** בשום פריים, רקע, מסך או פוסטר.
  - **אין קול אישה** בשום מקום: שירה, ראפ, דיבור, קריינות, סמפל או קול רקע.
  - האיסור חל על **כל תוצר בריפו**, לא רק על הקליפ — גם בדיקות, דוגמאות,
    מדריכים ומחקרים. אין להשתמש בקול אישה אפילו כדי להדגים יכולת או להשוות
    בין כלים.
  - קול ברירת המחדל הוא גברי (`am_michael`). `tools/tts.py` חוסם מזהי-קול
    נשיים מעצמו ונכשל בשגיאה — אין לעקוף אותו.
  - ⚠️ **`tts.py` מכסה רק את Kokoro.** קול שמגיע מ-Lyria או מ-Gemini TTS אינו
    עובר דרכו, ולכן נבדק **במדידה על האודיו עצמו** — הנוהל בסעיף 8
    ב-`מסמכים/6. הנחיות הפקה מומלצות.md`.
- **שירה באנגלית. כתוביות בעברית בלבד.**
- **85–95 שניות.** אין placeholders. אין פריימים שחורים.
- **לא לעצור לשאלות.** בבלוקר — לעבור לחלופה, לתעד, ולהמשיך.
- **מילים מודגשות** (10%, TOKENS, BATTERY) = שכבת גרפיקה נפרדת, **לא** בתוך
  שורת כתובית עברית.
- **אין חיקוי של אדם מפורסם אמיתי.**

**הבריף היצירתי** (שיר, בדיחה, דמות, מבנה): `מסמכים/1. הנחיה מקורית.md` ·
`מסמכים/2. סיכום שיחה עם קלוד.md`

**שכבת הכלים** (מה מפעילים ובכמה זה עולה): `מסמכים/5. מחקר כלים ליצירת קליפ מוזיקלי.md`
(המחקר — *למה*) · `מסמכים/6. הנחיות הפקה מומלצות.md` (הפקודות — *מה לעשות*) ·
`מסמכים/7. מה לבדוק ולחבר — צ׳קליסט לבעלים.md` (מה שרק הבעלים יכול לעשות).
ראיות מדודות: `tests/research/`.

---

## 2. שפה ותקשורת

- **שפה פשוטה וברורה לבעלים**, בלי ז'רגון; מונח טכני מקבל הסבר קצר לצידו.
  הוראה לפעולה בכלי חיצוני כוללת את שם הכפתור כפי שהוא מופיע בממשק בפועל.
- **הגנה על פרטיות** — אין לחשוף שם, מייל או טלפון אמיתיים של הבעלים בשום
  תוצר, קרדיט, מטא-דאטה או פוסט.

---

## 3. הסביבה — ענן, וחד-פעמית

Ubuntu בענן: 4 ליבות, 15GB זיכרון, בלי GPU. **כל ההתקנות (~6GB: ffmpeg,
torch, Kokoro, פונטים) נמחקות בסוף כל סשן. הריפו הוא הדבר היחיד ששורד.**

```bash
bash tools/bootstrap.sh   # ראשון בכל סשן, לפני כל דבר אחר
```

משם נובעות שתי חובות שאין עליהן ויכוח — פרקים 7 ו-8.

---

## 4. שכבת ההפקה

**שכבה C — נתיב פרוצדורלי מלא.** אין GPU, ולכן אין ComfyUI, אין Stable
Diffusion, אין Wan/Flux, ואין מודל וידאו. כל וידאו נבנה מסטילס + פילטרים.

- **שוטים:** HTML/CSS → PNG דרך Chromium (`tools/shot.js`).
  Chromium מותקן מראש ב-`/opt/pw-browsers` — **אין להריץ `playwright install`**.
- **תנועה:** פילטרים של ffmpeg (Ken Burns, זום, whip pan, glitch, RGB split, shake).
- **אודיו:** `tools/tts.py` (Kokoro TTS, קול) + `tools/beat.py` (ביט פרוצדורלי).

**זמני ריצה שנמדדו כאן** — לתכנון, לא להערכה בעל פה:

| פעולה | זמן |
|---|---|
| רינדור 30 שניות (פילטרים + x264 medium) | 83 שניות = 2.8× זמן אמת |
| מזה פילטרים / מזה קידוד | 48 שניות / 35 שניות |
| מעבר מלא על 90 שניות | ~4.2 דקות |
| צילום שוט אחד ב-Chromium | 9.1 שניות |

---

## 5. עברית RTL — הכלל הכי חשוב בריפו

**libass מקורי + כפיית כיוון-בסיס RTL ב-`U+202B` (RLE) … `U+202C` (PDF).**

ffmpeg כאן נבנה עם `--enable-libass --enable-libfribidi --enable-libharfbuzz`,
ו-libass **כן** מיישם bidi. הכשל היחיד היה כיוון-בסיס: הוא נקבע כ-LTR, ולכן
פיסוק בקצה השורה (`.` `!` `?`) נחת בקצה הימני במקום השמאלי.

🔴 **אין להריץ `python-bidi` / `get_display()` על הטקסט.** נמדד: זה גורם היפוך
כפול והעברית נקראת הפוך. python-bidi מיועד לרנדרר שאין לו bidi; libass אינו כזה.

🔴 **אין להכניס תגי עיצוב** (`{\c...}`, `{\b1}`) **בתוך שורת כתובית עברית** —
הם מפצלים את השורה לריצות bidi נפרדות ושוברים את סדר המילים. זו בדיוק הסיבה
שמילים מודגשות הן שכבת גרפיקה נפרדת.

**כל שורת כתובית נכתבת דרך `tools/heb_ass.py`** — הוא עוטף בכיוון RTL ומבריח
תווים אוטומטית. אין לכתוב שורות `Dialogue:` ביד.

---

## 6. אימות — לא להאמין לעין

**בדיקה חזותית של טקסט דו-כיווני אינה אמינה.** פריים יכול "להיראות תקין"
בזמן שהפיסוק בצד הלא נכון. גרוע מזה: בדיקות מדידה החזירו כאן תוצאה שגויה
שלוש פעמים כי **הבדיקות עצמן** היו פגומות — אחת מיזגה את הנקודה לתוך המילה,
אחרת הניחה ש-`1` צר מ-`0` בפונט **מונוספייס**, שבו כל התווים ברוחב זהה.

> **לאמת את הבדיקה לפני שמאמינים לפסק הדין שלה.**

בדיקות חובה לפני סגירת כל שינוי:

```bash
python3 tools/rtl_probe.py Rubik      # שער רגרסיה ל-bidi, בהשוואת גליפים
bash tests/smoke.sh                   # שרשרת מלאה: וידאו+אודיו+כתובית+גרפיקה
```

**שוט או קטע לא גמור בלי תוצר אמיתי שמוכיח שהוא עובד** — לא מספיק שהפקודה
רצה בהצלחה. פריים שחור, אודיו שקט או קובץ באורך 0 הם כישלון, גם אם ffmpeg
החזיר 0. לבדוק עם `ffprobe`, ולפתוח פריים ולהסתכל.

---

## 7. שמירת התקדמות — חובה, לא המלצה

הקונטיינר נמחק. סשן עלול להיקטע באמצע. **כל תוצר שלא נדחף לריפו נעלם.**

### הכלל

> **אחרי כל שוט שצולם, כל קטע שרונדר, כל קובץ אודיו שנוצר — צ'קפוינט.**

```bash
bash tools/checkpoint.sh "12 שוטים צולמו"
```

הכלי מוסיף שורה ליומן שב-`PROGRESS.md`, מסנכרן מול main, ודוחף. אם סשן אחר
הקדים — הוא מסנכרן ומנסה שוב, עד 5 פעמים.

קומיט עולה שניות. סשן שנקטע בלי צ'קפוינט עולה שעות. **עדיף פעם ביותר מדי.**

### מה נשמר בגיט

**הכל.** שוטים, אודיו, קטעי וידאו, קבצי כתוביות — כולם נכנסים לריפו.
`.gitignore` חוסם רק `__pycache__` וקבצים זמניים. זו החלטה מודעת: בסביבה
חד-פעמית, נכס שלא בגיט הוא נכס שלא קיים.

### רינדור בקטעים, לא במכה אחת

**אין לרנדר 90 שניות ברצף.** לרנדר קטע-קטע ל-`output/segments/`, ולעשות
צ'קפוינט אחרי כל קטע. בסוף לשרשר ל-`output/final.mp4`.

הסיבה: מעבר מלא לוקח ~4.2 דקות, וההפקה דורשת 8–15 מעברים. סשן שנקטע
באמצע רינדור רציף מאבד הכול; סשן שנקטע אחרי קטע 7 ממשיך מקטע 8.

לפני רינדור קטע — לבדוק אם הוא כבר קיים ב-`output/segments/` ולדלג עליו.

### עבודה ארוכה

תוצרי ביניים נשמרים לקבצים תוך כדי, לא נצברים בזיכרון הסשן.
אם נעצרים על מגבלת שימוש — לרשום ב-`PROGRESS.md` איפה בדיוק נעצרנו.

---

## 8. סנכרון בין סשנים ואנשים

סשנים ואנשים עובדים במקביל. בלי סנכרון, שניים מייצרים את אותו שוט ואיש
לא יודע מה מצב ההפקה.

### `PROGRESS.md` הוא מקור האמת

הלוח החי של ההפקה: מה הושלם, מה תפוס עכשיו ועל ידי מי, ומה השלב הפנוי הבא.
**אין לערוך אותו ביד** — הוא מתעדכן דרך `claim.sh` ו-`checkpoint.sh` בלבד.

### לתפוס שלב לפני העבודה, לא אחריה

```bash
bash tools/claim.sh 8              # תפיסת שלב 8 — לפני הפעולה הראשונה
bash tools/claim.sh 8 --done       # סיום
bash tools/claim.sh 8 --blocked "הסיבה"
bash tools/claim.sh 8 --free       # שחרור תפיסה תקועה
```

התפיסה נדחפת ל-main **מיד**, לפני שורת העבודה הראשונה. תפיסה שקורית בסוף
היא חסרת ערך: כל הזמן שעבר, סשן אחר ראה את השלב כפנוי.

**אין לגעת בשלב שמסומן `🔵 בעבודה` ע"י סשן אחר.** `claim.sh` יסרב מעצמו.

**תפיסה תקועה:** סשן שנקטע משאיר שלב תפוס לנצח. אם "עודכן" ישן משעתיים
ואין התקדמות ביומן הצ'קפוינטים — לשחרר ב-`--free` ולציין זאת בסיכום.

### התנגשויות

`checkpoint.sh` ו-`claim.sh` מסנכרנים מול main אוטומטית לפני כל דחיפה.
- **עריכות מקבילות שאינן סותרות** → נפתרות לבד, שני הצדדים נשמרים, ומדווח בסיכום.
- **התנגשות אמיתית** (שני שינויים שלא יכולים לדור בכפיפה אחת) → לעצור,
  להסביר בפשטות מה מתנגש ובאיזה קובץ, ולבקש הכרעת בעלים.

### מה הולך ישירות ל-main ומה דרך PR

| סוג שינוי | דרך |
|---|---|
| תוצרי הפקה (שוטים, אודיו, קטעים, `PROGRESS.md`) | **ישירות ל-main**, בצ'קפוינטים תכופים |
| כלים, הנחיות, `CLAUDE.md`, סקריפטים | ענף עבודה → PR → מיזוג |

תוצרים אינם עוברים ביקורת קוד, וצ'קפוינט כל כמה דקות דרך PR הוא חסר טעם.
כלים כן — הם משפיעים על כל סשן עתידי.

---

## 9. פתיחת סשן וסיומו

**בפתיחה** `.claude/hooks/session-start.sh` רץ אוטומטית ומדפיס: מצב Git,
האם הסביבה מותקנת, מה תפוס כרגע, ומה השלב הפנוי הבא. אם משום מה לא רץ —
להריץ ידנית. אחריו:

1. `bash tools/bootstrap.sh` אם הסביבה חסרה.
2. לקרוא את `PROGRESS.md`.
3. לתפוס שלב פנוי ב-`claim.sh`.

**בסיום**
- צ'קפוינט אחרון, ולוודא ש-`git status` נקי.
- לסמן `--done` על מה שהושלם, או `--free` על מה שנעזב באמצע.
- **תיעוד ותוצר לא מתפצלים** — אם ההנחיות לא עודכנו, המשימה לא גמורה.
- לדווח לבעלים בעברית: מה הושלם, מה נשאר, ואיפה בדיוק נעצרנו.

---

## 10. הכלים

| כלי | תפקיד |
|---|---|
| `tools/bootstrap.sh` | שחזור כל הסביבה — **ראשון בכל סשן** |
| `tools/checkpoint.sh` | שמירת ההתקדמות לריפו — **אחרי כל שלב** |
| `tools/claim.sh` | תפיסת שלב ושחרורו — **לפני כל שלב** |
| `tools/heb_ass.py` | בניית ASS עם עברית תקינה (RTL כפוי + בריחת תווים) |
| `tools/rtl_probe.py` | שער רגרסיה ל-bidi בהשוואת גליפים |
| `tools/shot.js` | HTML → PNG 1920x1080 דרך Chromium (כולל רקע שקוף) |
| `tools/beat.py` | ביט הִיפ-הופ פרוצדורלי, דטרמיניסטי, 150 BPM |
| `tools/tts.py` | Kokoro TTS מקומי (CPU), קול גברי אמריקאי |
| `tools/yt_auth.py` | הרשאת OAuth ליוטיוב — פותחת כתיבת תגובות והעלאה |
| `tools/yt_comments.py` | קריאת תגובות (מפתח API) · כתיבה ותשובות (OAuth) |
| `tools/yt_upload.py` | העלאה ליוטיוב — **נעולה מאחורי `--i-know-the-risk`** |

⚠️ **העלאה דרך ה-API נועלת את הסרטון כ"פרטי" לצמיתות** אם הפרויקט לא עבר
ביקורת תאימות של יוטיוב, ואין ערעור. ברירת המחדל היא להעלות ידנית דרך יוטיוב
סטודיו. פתיחת ערוץ אינה אפשרית ב-API כלל. הפרטים והנימוקים:
`מסמכים/4. יוטיוב — מחקר, כלים ותהליך הרשאה.md`. הבדיקה: `bash tests/yt_smoke.sh`.

**לפני שיוצרים כלי, נכס או קובץ עזר חדש — לחפש בפועל אם כבר קיים כזה**, לפי
כמה ניסוחים. חיפוש יחיד שיוצא ריק אינו הוכחה. קיים ⇒ להשתמש או להרחיב.

---

## 11. פונטים

ב-`fonts/` **וגם** מותקנים ברמת מערכת ב-`/usr/local/share/fonts/music-clip/`
(`bootstrap.sh` עושה זאת). Rubik (כתוביות) · Noto Sans Hebrew (גיבוי) ·
JetBrains Mono (שכבת גרפיקה). כולם משתנים (variable). רישיונות ב-`fonts/licenses/`.

⚠️ **אין לשים קבצים שאינם פונטים ב-`fonts/`** — libass מנסה לטעון כל קובץ שם
ומרעיש שגיאות. הרישיונות יושבים ב-`fonts/licenses/` בדיוק מהסיבה הזאת.

---

## 12. מבנה התיקיות

| תיקייה | תוכן |
|---|---|
| `assets/reference` | רפרנסים |
| `assets/shots` | שוטים מוגמרים (ו-`html/` למקורות) |
| `assets/audio` | אודיו |
| `subs` | כתוביות |
| `tools` | כלי ההפקה |
| `tests` | בדיקות ותוצריהן |
| `output/segments` | קטעי וידאו מרונדרים |
| `output` | `final.mp4` |
| `logs` | יומני ריצה |
| `מסמכים` | הנחיות הפרויקט |
