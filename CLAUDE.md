# הנחיות עבודה — Content-creator

ריפו להפקת תוכן (וידאו, אודיו, ויזואליה) בכלים מקומיים וחינמיים.
**העבודה מתבצעת בסשן ענן.** הנימוק המלא: `מסמכים/3. למה הפקה בענן.md`.

---

## 1. אילוצים קשיחים

### תקציב — 🟢 מסלול B מאושר (13.8.2026)

**הבעלים אישר מסלול B: תקרה ~$1.50 (~₪7) לקליפ שלם.**

| מותר | אסור |
|---|---|
| ✅ **Lyria** — שיר אחד (~$0.08) | 🔴 **Veo** — אישור **בכתב** בלבד. 90 שניות = $4.50–$54 |
| ✅ **תמונות Gemini** — עד 30 (~$0.05 לתמונה) | 🔴 ללחוץ **"שדרג"** או **"הפעלת חיוב"** בשום מסך |
| ✅ **Gemini TTS** · flash-lite לשיפוט | 🔴 לחרוג מהתקרה בלי אישור חדש |

⚠️ **שום דבר כאן אינו "חינם" באמת.** המפתח שבסביבה שייך לפרויקט ב-**Tier 1**
(חשבון חיוב מקושר), ולכן עמודת "Free of charge" בדף התמחור של Google **אינה חלה
עליו** — גם TTS וגם flash-lite מחויבים, באגורות בודדות. קרדיט ה-₪921 של הבעלים
יושב בחשבון אחר ואינו יכול לכסות Gemini API. הרקע המלא: מסמך 5 סעיף 0.2.

**חריגה מהתקרה = בלוקר.** לתעד, לעבור לגיבוי (`beat.py` + Kokoro), ולהמשיך.

### ויזואליה

- 🔴 **התמונות חייבות להיראות ריאליסטיות** — צילום, לא ציור ולא אנימה.
  זו דרישת בעלים מפורשת. **נמדד:** Pollinations מחזיר סגנון ציורי ב-1024×576
  ומתעלם מהפרמטרים שנשלחים אליו ⇒ **אינו עומד בדרישה.**
  המקור המאושר: `gemini-3.1-flash-image` (Nano Banana). ראיה: `tests/research/`.
- **Full HD חובה** — כל שוט וכל תוצר סופי ב-1920×1080. תמונה שחוזרת קטנה יותר
  מותאמת מיד (`scale` + `crop`), ותמונה שאי אפשר להביא ל-1080p נפסלת.
- **אין חיקוי של אדם מפורסם אמיתי.**

### 🔴 איסור גורף על נשים — חזותית וקולית

- **אין נשים** בשום פריים, רקע, מסך או פוסטר. **אין קול אישה** בשום מקום:
  שירה, ראפ, דיבור, קריינות, סמפל או קול רקע.
- האיסור חל על **כל תוצר בריפו** — גם בדיקות, דוגמאות ומחקרים. אין להשתמש
  בקול אישה אפילו כדי להדגים יכולת.
- `tools/tts.py` חוסם מזהי-קול נשיים מעצמו — אין לעקוף. **אבל הוא מכסה רק את
  Kokoro.** קול מ-Lyria או מ-Gemini TTS נבדק **במדידה על האודיו עצמו** —
  הנוהל בסעיף 8 ב-`מסמכים/6`.
- כל תמונה שנוצרת — **לפתוח ולראות בעין** לפני שהיא נכנסת לריפו.

### תוצר

- **85–95 שניות.** אין placeholders. אין פריימים שחורים.
- **שירה באנגלית. כתוביות בעברית בלבד.**
- **מילים מודגשות** (10%, TOKENS, BATTERY) = שכבת גרפיקה נפרדת, **לא** בתוך
  שורת כתובית עברית. הסיבה בפרק 5.
- **לא לעצור לשאלות.** בבלוקר — לעבור לחלופה, לתעד ב-`logs/decisions.log`, ולהמשיך.

**הבריף היצירתי** (שיר, בדיחה, דמות, מבנה): `מסמכים/1. הנחיה מקורית.md` ·
`מסמכים/2. סיכום שיחה עם קלוד.md` — ⚠️ **קרא קודם את באנר התיקון בראש מסמך 2.**

**שכבת הכלים:** `מסמכים/5` (המחקר — *למה*) · `מסמכים/6` (הפקודות — *מה לעשות*) ·
`מסמכים/7` (מה שרק הבעלים יכול לעשות). ראיות מדודות: `tests/research/`.

---

## 2. שפה ותקשורת

- **שפה פשוטה וברורה לבעלים**, בלי ז'רגון; מונח טכני מקבל הסבר קצר לצידו.
  הוראה לפעולה בכלי חיצוני כוללת את שם הכפתור כפי שהוא מופיע בממשק בפועל.
- **הגנה על פרטיות** — אין לחשוף שם, מייל או טלפון אמיתיים של הבעלים בשום
  תוצר, קרדיט, מטא-דאטה או פוסט.

---

## 3. הסביבה — ענן, וחד-פעמית

Ubuntu בענן: 4 ליבות, 15GB זיכרון, בלי GPU. **כל ההתקנות (~6GB) נמחקות בסוף כל
סשן. הריפו הוא הדבר היחיד ששורד.**

```bash
bash tools/bootstrap.sh   # ראשון בכל סשן, לפני כל דבר אחר
```

משם נובעות שתי חובות שאין עליהן ויכוח — פרקים 7 ו-8.

---

## 4. שכבת ההפקה

**אין GPU**, ולכן אין ComfyUI, אין Stable Diffusion מקומי ואין מודל וידאו שרץ כאן.
כל וידאו נבנה מסטילס + פילטרים. **מעבר רינדור מלא על 90 שניות ≈ 4.2 דקות**
(המדידות המלאות: `מסמכים/3`).

| שכבה | הקו הראשי | הגיבוי |
|---|---|---|
| **שיר ושירה** | **Lyria 3** — שירה אמיתית + חותמות זמן לשורות | `beat.py` + `tts.py` (ראפ מדובר) |
| **קול מדובר** | **Gemini TTS** (קול `Charon`) | Kokoro (`tools/tts.py`) |
| **מסכים וטקסט** | **HTML/CSS → PNG** (`tools/shot.js`) | — |
| **דמות וסצנות** | **Nano Banana** (`gemini-3.1-flash-image`) | Cloudflare flux (דורש אסימון) |
| **תנועה** | פילטרים של ffmpeg | שוטי מלאי מ-Pexels |

> **הכלל:** טקסט על המסך ⇒ **HTML**. בן אדם או סצנה ⇒ **מודל תמונה**.
> מודל תמונה מייצר טרמינל עם טקסט מעוות; HTML נותן `10%` מושלם.

**נעילת דמות:** לשלוח את תמונת הרפרנס עצמה יחד עם הפרומפט ולבקש שינוי
תנוחה/זווית. נעילה חזקה יותר מ-seed. הפקודות המלאות: `מסמכים/6` סעיפים 2–4.

⚠️ **חותמות הזמן של Lyria נבדקות מול האודיו לפני שבונים עליהן כתוביות.**
בדגימה שבריפו הן מרווחות בדיוק 3.2 שניות — ייתכן שזו רשת נומינלית ולא הצמדה
לשירה בפועל. הכלל של פרק 6 חל גם עליהן.

Chromium מותקן מראש ב-`/opt/pw-browsers` — **אין להריץ `playwright install`**.

---

## 5. עברית RTL — הכלל הכי חשוב בריפו

**libass מקורי + כפיית כיוון-בסיס RTL ב-`U+202B` (RLE) … `U+202C` (PDF).**

ffmpeg כאן נבנה עם `--enable-libass --enable-libfribidi --enable-libharfbuzz`,
ו-libass **כן** מיישם bidi. הכשל היחיד היה כיוון-בסיס: הוא נקבע כ-LTR, ולכן
פיסוק בקצה השורה (`.` `!` `?`) נחת בקצה הימני במקום השמאלי.

🔴 **אין להריץ `python-bidi` / `get_display()` על הטקסט.** נמדד: זה גורם היפוך
כפול והעברית נקראת הפוך. python-bidi מיועד לרנדרר שאין לו bidi; libass אינו כזה.

🔴 **אין להכניס תגי עיצוב** (`{\c...}`, `{\b1}`) **בתוך שורת כתובית עברית** —
הם מפצלים את השורה לריצות bidi נפרדות ושוברים את סדר המילים. **זו הסיבה
האמיתית** לכך שמילים מודגשות הן שכבת גרפיקה נפרדת.

ℹ️ **דיוק:** `10%` בתוך שורה עברית **אינו** מתהפך ל-`%10` — נמדד ב-`tests/rtl_mixed.png`,
ותקן היוניקוד קובע ש-`%` צמוד לספרה נבלע לתוך ריצת המספר. הכלל של שכבה נפרדת
נשאר בתוקף (תגי עיצוב + מראה), אבל לא מהנימוק הזה.

**כל שורת כתובית נכתבת דרך `tools/heb_ass.py`** — הוא עוטף בכיוון RTL ומבריח
תווים אוטומטית. אין לכתוב שורות `Dialogue:` ביד.
**שורות קצרות: עד 38 תווים, מקסימום 2 שורות.** שורה שנשברת אוטומטית לא נבדקה —
לשבור ידנית ב-`\N` ולהריץ עליה `rtl_probe`.

---

## 6. אימות — לא להאמין לעין

**בדיקה חזותית של טקסט דו-כיווני אינה אמינה.** פריים יכול "להיראות תקין"
בזמן שהפיסוק בצד הלא נכון. גרוע מזה: בדיקות מדידה החזירו כאן תוצאה שגויה
ארבע פעמים כי **הבדיקות עצמן** היו פגומות — אחת מיזגה את הנקודה לתוך המילה,
אחרת הניחה ש-`1` צר מ-`0` בפונט **מונוספייס**, ואחת מדדה תדר יסוד על מיקס מלא
והסיקה "קול אישה" מסינתיסייזרים.

> **לאמת את הבדיקה לפני שמאמינים לפסק הדין שלה.**
> ולהריץ תמיד גם קובץ בקרה שהתשובה עליו ידועה.

בדיקות חובה לפני סגירת כל שינוי:

```bash
python3 tools/rtl_probe.py Rubik      # שער רגרסיה ל-bidi, בהשוואת גליפים
bash tests/smoke.sh                   # שרשרת מלאה: וידאו+אודיו+כתובית+גרפיקה
```

**שוט או קטע לא גמור בלי תוצר אמיתי שמוכיח שהוא עובד** — לא מספיק שהפקודה
רצה בהצלחה. פריים שחור, אודיו שקט או קובץ באורך 0 הם כישלון, גם אם ffmpeg
החזיר 0. לבדוק עם `ffprobe`, ולפתוח פריים ולהסתכל.

---

## 7. שמירת התקדמות — חובה, לא המלצה

הקונטיינר נמחק. סשן עלול להיקטע באמצע. **כל תוצר שלא נדחף לריפו נעלם.**

> **אחרי כל שוט שצולם, כל קטע שרונדר, כל קובץ אודיו שנוצר — צ'קפוינט.**

```bash
bash tools/checkpoint.sh "12 שוטים צולמו"
```

הכלי מוסיף שורה ליומן שב-`PROGRESS.md`, מסנכרן מול main, ודוחף. אם סשן אחר
הקדים — הוא מסנכרן ומנסה שוב. **קומיט עולה שניות; סשן שנקטע בלי צ'קפוינט עולה
שעות. עדיף פעם ביותר מדי.**

**מה נשמר בגיט: הכל.** שוטים, אודיו, קטעי וידאו, כתוביות. `.gitignore` חוסם רק
`__pycache__` וקבצים זמניים. זו החלטה מודעת: בסביבה חד-פעמית, נכס שלא בגיט הוא
נכס שלא קיים. 🔴 **אין להוסיף תוצרי הפקה ל-`.gitignore`.**

**רינדור בקטעים, לא במכה אחת.** לרנדר ל-`output/segments/` ולעשות צ'קפוינט אחרי
כל קטע; בסוף לשרשר ל-`output/final.mp4`. לפני רינדור קטע — לבדוק אם הוא כבר קיים
ולדלג עליו. הסיבה: ההפקה דורשת 8–15 מעברים, וסשן שנקטע באמצע רינדור רציף
מאבד הכול.

תוצרי ביניים נשמרים לקבצים תוך כדי, לא נצברים בזיכרון הסשן. אם נעצרים על מגבלת
שימוש — לרשום ב-`PROGRESS.md` איפה בדיוק.

---

## 8. סנכרון בין סשנים ואנשים

### `PROGRESS.md` הוא מקור האמת

הלוח החי: מה הושלם, מה תפוס עכשיו ועל ידי מי, ומה השלב הפנוי הבא.
**אין לערוך אותו ביד** — הוא מתעדכן דרך `claim.sh` ו-`checkpoint.sh` בלבד.

### לתפוס שלב לפני העבודה, לא אחריה

```bash
bash tools/claim.sh 8              # תפיסת שלב — לפני הפעולה הראשונה
bash tools/claim.sh 8 --done       # סיום
bash tools/claim.sh 8 --blocked "הסיבה"
bash tools/claim.sh 8 --free       # שחרור תפיסה תקועה
```

התפיסה נדחפת ל-main **מיד**. תפיסה שקורית בסוף היא חסרת ערך.
**אין לגעת בשלב שמסומן `🔵 בעבודה`** — `claim.sh` יסרב מעצמו.
**תפיסה תקועה:** "עודכן" ישן משעתיים בלי התקדמות ביומן ⇒ לשחרר ב-`--free`
ולציין בסיכום.

### התנגשויות

- **עריכות מקבילות שאינן סותרות** → נפתרות לבד, שני הצדדים נשמרים, ומדווח בסיכום.
- **התנגשות אמיתית** (שני שינויים שלא יכולים לדור בכפיפה אחת) → לעצור, להסביר
  בפשטות מה מתנגש ובאיזה קובץ, ולבקש הכרעת בעלים.

### מה הולך ישירות ל-main ומה דרך PR

| סוג שינוי | דרך |
|---|---|
| תוצרי הפקה (שוטים, אודיו, קטעים, `PROGRESS.md`) | **ישירות ל-main**, בצ'קפוינטים תכופים |
| כלים, הנחיות, `CLAUDE.md`, סקריפטים | ענף עבודה → PR → מיזוג |

---

## 9. פתיחת סשן וסיומו

**בפתיחה** `.claude/hooks/session-start.sh` רץ אוטומטית ומדפיס מצב Git, מצב
הסביבה, מה תפוס ומה הבא. אחריו: `bootstrap.sh` → לקרוא `PROGRESS.md` → `claim.sh`.

**בסיום:** צ'קפוינט אחרון · `git status` נקי · `--done` או `--free` על מה שנתפס ·
**תיעוד ותוצר לא מתפצלים** (הנחיות לא עודכנו = המשימה לא גמורה) · ולדווח לבעלים
בעברית מה הושלם, מה נשאר, ואיפה בדיוק נעצרנו.

---

## 10. הכלים

| כלי | תפקיד |
|---|---|
| `tools/bootstrap.sh` | שחזור כל הסביבה — **ראשון בכל סשן** |
| `tools/checkpoint.sh` | שמירת ההתקדמות לריפו — **אחרי כל שלב** |
| `tools/claim.sh` | תפיסת שלב ושחרורו — **לפני כל שלב** |
| `tools/heb_ass.py` | בניית ASS עם עברית תקינה (RTL כפוי + בריחת תווים) |
| `tools/rtl_probe.py` | שער רגרסיה ל-bidi בהשוואת גליפים |
| `tools/shot.js` | HTML → PNG 1920x1080 דרך Chromium (כולל רקע שקוף) |
| `tools/band.py` | ליווי בכלים מוקלטים אמיתיים — MMA + סאונדפונט MIT |
| `tools/synth.py` | ספריית ה-DSP המשותפת |
| `tools/beat.py` | ביט פרוצדורלי דטרמיניסטי. `--style rap\|pop\|house\|legacy` |
| `tools/chords.py` | הרמוניה ומלודיה פרוצדורליות |
| `tools/mixdown.py` | מיקס ומאסטר לסטמים + יעד עוצמה אוטומטי |
| `tools/loudness.py` | מד עוצמה BS.1770 + `--balance` מול הפקה אמיתית |
| `tools/tts.py` | Kokoro TTS מקומי (CPU), קול גברי — **גיבוי** |
| `tools/yt_*.py` | יוטיוב: הרשאה, תגובות והעלאה. ⚠️ **לקרוא `מסמכים/4` לפני שנוגעים** |

🎵 **כלי האודיו הם שרשרת אחת:** `band.py` + `beat.py` + `chords.py` →
`mixdown.py` → `loudness.py` כשער.

🔴 **לא לסנתז מה שאפשר לנגן.** `band.py` משתמש ב-MMA (1138 סגנונות ליווי מוכנים)
ובסאונדפונט FluidR3 GM — הקלטות של כלים אמיתיים, MIT, חינם גם מסחרית.
**חלוקת העבודה:** כלים אקוסטיים ← `band.py` · תופים אלקטרוניים ו-808 ← `beat.py` ·
פדים וסטאבים סינתטיים ← `chords.py`.

🔴 **הבעיה כמעט תמיד במיקס ובסידור, לא בסינתזה.** האבחון:
```bash
python3 tools/loudness.py mix.wav --balance   # מול פרופיל של הפקה אמיתית
```
שלוש הסיבות שחוזרות: **פלט מונו · באס אוקטבה גבוה מדי · לולאה זהה בלי מבנה.**
שלושתן נפתרות בכלים, לא ב-EQ. המספרים המדודים והסגנונות: `מסמכים/מחקר/`.
הבדיקה: `bash tests/audio_smoke.sh`.

**לפני שיוצרים כלי, נכס או קובץ עזר חדש — לחפש בפועל אם כבר קיים כזה**, לפי
כמה ניסוחים. חיפוש יחיד שיוצא ריק אינו הוכחה. קיים ⇒ להשתמש או להרחיב.

---

## 11. פונטים

ב-`fonts/` **וגם** מותקנים ברמת מערכת (`bootstrap.sh` עושה זאת):
Rubik (כתוביות) · Noto Sans Hebrew (גיבוי) · JetBrains Mono (שכבת גרפיקה).

⚠️ **אין לשים קבצים שאינם פונטים ב-`fonts/`** — libass מנסה לטעון כל קובץ שם
ומרעיש שגיאות. הרישיונות יושבים ב-`fonts/licenses/` בדיוק מהסיבה הזאת.

---

## 12. מבנה התיקיות

| תיקייה | תוכן |
|---|---|
| `assets/reference` | רפרנס הדמות — התמונה שנשלחת חזרה לנעילת עקביות |
| `assets/stills` | סטילס גולמיים ממודל תמונה (`_hd` = אחרי התאמה ל-1080p) |
| `assets/shots` | שוטים מוגמרים (ו-`html/` למקורות ה-HTML) |
| `assets/audio` | אודיו |
| `subs` | כתוביות |
| `tools` · `tests` | כלי ההפקה · בדיקות ותוצריהן |
| `output/segments` · `output` | קטעי וידאו מרונדרים · `final.mp4` |
| `logs` | יומני ריצה, כולל `decisions.log` |
| `מסמכים` · `מסמכים/מחקר` | הנחיות הפרויקט · מחקר מוזיקה 2026 |
