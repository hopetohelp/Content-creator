# הנחיות הפקה מומלצות — קליפ מוזיקלי

**נגזר מ-`5. מחקר כלים ליצירת קליפ מוזיקלי.md`.** המסמך ההוא מסביר *למה*; המסמך הזה
אומר *מה לעשות*. כל פקודה כאן הורצה בפועל בסביבת הפרויקט, אלא אם צוין אחרת.

**מה זה לא:** זה לא מחליף את `מסמכים/2. סיכום שיחה עם קלוד.md`. הבריף היצירתי —
השיר, הבדיחה, הדמות, המבנה, כללי האנטי-cringe — נשאר שם ותקף במלואו.
המסמך הזה מחליף רק את **שכבת הכלים**.

---

## 0. שער תקציב

## 🟢 הוכרע 13.8.2026 — הבעלים אישר מסלול B

**תקרה: ~$1.50 (~₪7) לקליפ שלם**, מיתרת התשלום מראש של הפרויקט
"הפרויקט הראשון שלי" (Tier 1). קרדיט ה-₪921 יושב בחשבון אחר ואינו יכול לכסות
Gemini API — הבירור המלא בסעיף 0.2 של מסמך 5.

| | מותר? |
|---|---|
| **Lyria** — שיר אחד (~$0.08) | ✅ כן |
| **תמונות Gemini** — עד 30 (~$0.05 לתמונה) | ✅ כן |
| **Gemini TTS · flash-lite לשיפוט** | ✅ כן |
| **Veo** | 🔴 **לא** — אישור **בכתב** בלבד. $4.50–$54 ל-90 שניות |

⚠️ **אין כאן קריאה שהיא "חינם" ודאי.** "Free of charge" בדף התמחור חל על
**השכבה החינמית**, והמפתח שבסביבה הוא Tier 1 ⇒ גם TTS וגם flash-lite מחויבים
באגורות. זה בתוך התקרה — אבל לא לספור אותם כאפס.

🔴 **חריגה מהתקרה = בלוקר.** לתעד, לעבור לגיבוי (`beat.py` + Kokoro), ולהמשיך.
🔴 **אין ללחוץ "שדרג" או "הפעלת חיוב"** בשום מסך חיוב, לעולם.

<details>
<summary>המסלולים האחרים — לעיון בלבד, אינם בתוקף</summary>

---

**לפני שסשן הפקה נוגע ב-Gemini — הבעלים חייב לבחור מסלול, בכתב:**

| מסלול | תקרה | מה מותר |
|---|---|---|
| **A** | $0 | ‏Gemini **TTS בלבד** (חינם רשמית). אסור: תמונות, Lyria, Veo |
| **A+** | $0 | A, ובנוסף שיר ותמונות שהבעלים מייצר **ידנית בדפדפן** |
| **B** ⭐ | ~$1.50 | ‏TTS + Lyria (שיר אחד) + עד 30 תמונות. **אסור Veo** |
| **C** | לפי הצהרה מפורשת | + Veo, בתקרת שניות כתובה |

🔴 **Veo אסור בכל מסלול שלא אושר בכתב.** 90 שניות של Veo עולות בין $4.50 ל-$54.
**לעולם לא בסשן לא-מפוקח.**

### לפני שבוחרים — שתי בדיקות של שתי דקות

**1. יש קרדיטים חינמיים פעילים?** console.cloud.google.com → **Billing** → **Credits**.
יש יתרה מ-$300 הפתיחה ⇒ **מסלול B הוא $0 אמיתי** לפי כללי הפרויקט, שמתירים
`free credits` במפורש. אין יתרה ⇒ זה כסף אמיתי.

**2. רוצים ודאות מוחלטת?** להוציא מפתח בפרויקט **בלי חשבון חיוב**
(aistudio.google.com → Get API key) ולהחליף בו את משתנה הסביבה. אז חריגה
פשוט תיכשל בשגיאה במקום לחייב.
⚠️ אבל לדעת: **מפתח חינמי חוסם גם את Lyria וגם את התמונות** — הוא נועל אותך
למסלול A. המכסה נספרת לפי פרויקט, לא לפי מפתח.

### מסלול A+ — שיר של Lyria ב-$0, ידנית

אם אין קרדיטים ורוצים בכל זאת שיר אמיתי: הבעלים נכנס ל-**labs.google → Flow Music**
(קרדיטים יומיים חינם, בלי כרטיס אשראי), מייצר שיר עד 3 דקות, מוריד, ומפיל
ל-`assets/audio/song.mp3`. משם הפייפליין ממשיך רגיל.

**המחיר:** אין חותמות זמן אוטומטיות — צריך `stable-ts` כמו במסלול A.
**זכויות מסחריות לא נבדקו** — לבדוק לפני פרסום ציבורי.

</details>

---

## 1. סדר העבודה

```
0. bootstrap.sh + claim.sh          ← ראשון תמיד, לתפוס שלב לפני העבודה
1. שיר + שירה                        ← Lyria. גיבוי: beat.py + TTS
2. חותמות זמן לשורות                 ← מגיעות מ-Lyria — ולאמת אותן מול האודיו
3. תרגום הכתוביות לעברית             ← לפי סעיף 8 במסמך 2
4. סטילס של דמות ואובייקטים          ← Nano Banana. רפרנס אחד → נעילה לכל השאר
5. מסכים ב-HTML                      ← shot.js
6. שוטים: כל אחד ל-MP4 נפרד          ← ffmpeg, אף פעם לא רינדור אחד ארוך
7. שכבת גרפיקה (10% / TOKENS)        ← נפרדת מהכתובית. תמיד
8. concat + מיקס + כתוביות צרובות
9. QA חוסם                           ← סעיף 9 במסמך 2 + סעיף 10 כאן
```

🔴 **`checkpoint.sh` אחרי כל תוצר** — שוט שצולם, קובץ אודיו שנוצר, קטע שרונדר.
הקונטיינר נמחק; מה שלא נדחף, נעלם. פירוט מלא: פרקים 7–8 ב-`CLAUDE.md`.

---

## 2. שיר ושירה — Lyria 3 *(מסלולים B/C)*

**זו ההמלצה המרכזית של המחקר.** מחליף גם את `beat.py` וגם את `tts.py`,
ומבטל את כל שלב הסנכרון.

```bash
cat > /tmp/lyria.json <<'JSON'
{"contents":[{"parts":[{"text":"<תיאור סגנון + מילות השיר המלאות>"}]}]}
JSON

curl -s -X POST \
 "https://generativelanguage.googleapis.com/v1beta/models/lyria-3-clip-preview:generateContent?key=$GEMINI_API_KEY" \
 -H "Content-Type: application/json" -d @/tmp/lyria.json -o /tmp/lyria.json.out

# האודיו
jq -r '.candidates[0].content.parts[]|select(.inlineData)|.inlineData.data' \
  /tmp/lyria.json.out | base64 -d > assets/audio/song.mp3

# חותמות הזמן והמילים — לשמור! זה הבסיס לכתוביות
jq -r '.candidates[0].content.parts[]|select(.text)|.text' \
  /tmp/lyria.json.out > assets/audio/song_lyrics.txt
```

**מה חוזר ב-`song_lyrics.txt`:**
```
[0.0:]  Screen glows dim, a familiar dread
[3.2:]  Just a little warning that my power's in the red
[6.4:]  Ten on the battery? Bet.
[9.6:]  Ten on the tokens? Dead.
```
המספר = שנייה שבה השורה מתחילה. **כל שורה כזאת = כרטיס כתובית עברית אחד**
עם אותם זמנים בדיוק. אין מיפוי מילה-למילה (סדר המילים בעברית שונה).

🔴 **לאמת את החותמות מול האודיו לפני שבונים עליהן כתוביות.** בדגימה שבריפו
(`tests/research/lyria3_rap_lyrics.txt`) כל השורות מרווחות **בדיוק 3.2 שניות** —
זה יכול להיות סנכרון אמיתי, ויכול להיות רשת נומינלית שאינה נצמדת לשירה בפועל.
הבדיקה: לחלץ את השנייה שבה מתחילה השורה השנייה, להאזין/למדוד אנרגיה שם, ולוודא
שהשירה אכן מתחילה בנקודה הזאת. סטייה עקבית ⇒ להיעזר ב-`stable-ts` על השירה.
**זו בדיוק דוגמה לכלל "לאמת את הבדיקה לפני שמאמינים לפסק הדין שלה".**

**בפרומפט חובה לציין:** סגנון, ‎150 BPM half-time, **male vocals only**, ואת מילות השיר במלואן.

**מדוד תמיד אחרי:**
```bash
ffprobe -v error -show_entries format=duration -of csv=p=0 assets/audio/song.mp3
ffmpeg -hide_banner -i assets/audio/song.mp3 -af volumedetect -f null /dev/null 2>&1 | grep mean_volume
```
פחות מ-20 שניות, או ‎mean_volume מתחת ל-‎-40dB ⇒ כישלון. לייצר מחדש.

⚠️ `lyria-3-clip-preview` נמדד ב-**30.77 שניות**. ל-90 שניות: לנסות
`lyria-3-pro-preview` (**לא נבדק**, $0.08), או לתפור 3 קטעים. **לתפור רק אם התפר על תיבה.**

---

## 3. קול — Gemini TTS *(חינם בכל המסלולים)*

מחליף את Kokoro. איכות טובה יותר, בלי 330MB הורדה בכל סשן.

```bash
curl -s -X POST \
 "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-tts-preview:generateContent?key=$GEMINI_API_KEY" \
 -H "Content-Type: application/json" -d '{
  "contents":[{"parts":[{"text":"Say with a dry deadpan rapper delivery: <שורה>"}]}],
  "generationConfig":{"responseModalities":["AUDIO"],
    "speechConfig":{"voiceConfig":{"prebuiltVoiceConfig":{"voiceName":"Charon"}}}}}' \
 -o /tmp/tts.json

# מחזיר PCM גולמי — חייבים להמיר
jq -r '.candidates[0].content.parts[0].inlineData.data' /tmp/tts.json | base64 -d > /tmp/t.pcm
ffmpeg -v error -f s16le -ar 24000 -ac 1 -i /tmp/t.pcm assets/audio/line_01.wav -y
```

⚠️ **הפרמטרים `-f s16le -ar 24000 -ac 1` הם חובה** — המודל מחזיר PCM בלי כותרת.
בלעדיהם מקבלים רעש.

**מתי בכל זאת Kokoro:** אם Gemini נופל או אין רשת. `tools/tts.py` נשאר בריפו כגיבוי.

---

## 4. תמונות

> ### 🔴 דרישת בעלים: התמונות חייבות להיראות **ריאליסטיות**
> צילום, לא ציור ולא אנימה. ובנוסף — **Full HD (1920×1080) חובה בכל שוט.**
>
> | מקור | ריאליסטי? | מסקנה |
> |---|---|---|
> | **Nano Banana** | ✅ נמדד ונצפה — פוטוריאליסטי | **המקור המאושר** |
> | Pollinations | ❌ ציורי, 1024×576, מתעלם מהפרמטרים | **נפסל, גם כגיבוי לדמות** |
> | Cloudflare flux-schnell | ❓ לא נבדק מעולם | רק אחרי בדיקת פוטוריאליזם בעין |
>
> **בכל פרומפט לדמות:** `photorealistic, shot on 35mm, natural skin texture,
> cinematic lighting` — ו**לא** `illustration / anime / render / 3d`.
> תמונה שיוצאת ציורית **נפסלת ומיוצרת מחדש**, גם אם היא יפה.

### הקו הראשי — Nano Banana

```bash
curl -s -X POST \
 "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-image:generateContent?key=$GEMINI_API_KEY" \
 -H "Content-Type: application/json" \
 -d '{"contents":[{"parts":[{"text":"<שוט>. male character only, no women, no female characters, no background people. Cinematic, 16:9."}]}]}' \
 -o /tmp/img.json

jq -r '.candidates[0].content.parts[]|select(.inlineData)|.inlineData.data' /tmp/img.json \
  | base64 -d > assets/stills/shot_01.jpg
```

⚠️ **חוזר ב-1376×768, לא ב-1920×1080.** להתאים תמיד:
```bash
ffmpeg -i assets/stills/shot_01.jpg \
  -vf "scale=1920:1080:force_original_aspect_ratio=increase,crop=1920:1080" \
  assets/stills/shot_01_hd.jpg -y
```

**נעילת דמות:** Nano Banana יודע לערוך תמונה קיימת. במקום לקוות ל-seed —
לשלוח את תמונת הרפרנס יחד עם הפרומפט ולבקש שינוי תנוחה/זווית. **זו נעילה חזקה
יותר מכל מה שההנחיה המקורית תיארה.**

### גיבוי — Cloudflare Workers AI *(חינם, ~150–200 תמונות ביום)*

**הכנה חד-פעמית של הבעלים:** ב-dash.cloudflare.com → **My Profile** → **API Tokens**
→ **Create Token** → הרשאת **Workers AI**. את הערך שמתקבל לשמור כמשתנה סביבה
`CF_API_TOKEN`, ואת מזהה החשבון כ-`CF_ACCOUNT_ID`.

```bash
curl -s -X POST \
 "https://api.cloudflare.com/client/v4/accounts/$CF_ACCOUNT_ID/ai/run/@cf/black-forest-labs/flux-1-schnell" \
 -H "Authorization: Bearer $CF_API_TOKEN" -H "Content-Type: application/json" \
 -d '{"prompt":"<שוט>, male character only, no women","steps":4}' \
 | jq -r '.result.image' | base64 -d > assets/stills/shot_01.jpg
```
*(לא נבדק — לא היה אסימון בסביבה. **לאמת את התמונה הראשונה בעין** לפני שממשיכים.)*

### תמיד — מסכים ב-HTML, לא ב-AI

**כל שוט שיש בו טקסט נבנה ב-HTML/CSS ומצולם ב-`tools/shot.js`.**
מודל תמונה מייצר טרמינל עם טקסט מעוות ומספרים לא קריאים; HTML נותן `10%` מושלם.
טרמינל, IDE, מד טוקנים, סוללה, התראות — **לפחות 8 שוטים משם.**

> **הכלל:** טקסט על המסך ⇒ HTML · בן אדם או סצנה ⇒ מודל תמונה

### תוספת חינמית — שוטי מלאי מ-Pexels

וידאו אמיתי, רישיון מסחרי, בלי חובת ייחוס, ב-$0. ידיים על מקלדת, זוהר מסך,
חדר כהה — **לא מזהים דמות, ולכן לא נוגעים באילוץ העקביות**, ומוסיפים תנועת
מצלמה אמיתית שסטיל עם zoompan לא נותן. שווה 3–5 שוטים.

דורש `PEXELS_API_KEY` בסביבה. המכסה: 200 בקשות בשעה, 20,000 בחודש.

```bash
# חיפוש — רק לרוחב, ומסננים ל-Full HD בלבד
curl -s -H "Authorization: $PEXELS_API_KEY" \
 "https://api.pexels.com/videos/search?query=hands%20typing%20keyboard%20dark&orientation=landscape&size=medium&per_page=15" \
 | jq -r '.videos[] | .id as $id | .video_files[]
          | select(.width==1920 and .height==1080) | "\($id)\t\(.link)"' | head

# הורדה (להחליף <URL> בקישור מהשורה שנבחרה)
curl -sL "<URL>" -o assets/stills/stock_01.mp4
ffprobe -v error -show_entries stream=width,height,duration -of csv=p=0 assets/stills/stock_01.mp4
```

🔴 **שער חובה לפני שימוש בכל שוט מלאי:** מאגרי המלאי מלאים בבני אדם, וזה
**המקור המסוכן ביותר בפייפליין** לכלל "אין נשים" — מודל התמונה לפחות מקבל
הוראה, שוט מלאי לא. לכן, על כל קליפ שמורידים:

1. לחלץ פריים כל שנייה ולפתוח את כולם: `ffmpeg -i <קובץ> -vf fps=1 /tmp/f_%02d.png`
2. **לפסול כל קליפ שיש בו פנים, גוף או בבואה של אדם** שאינו הדמות הראשית.
3. להעדיף מלכתחילה שאילתות בלי בני אדם: `close up mechanical keyboard`,
   `computer screen code dark room`, `server rack lights` — ולא `programmer working`.

---

## 5. שוטים, תנועה והרכבה — בלי שינוי

הנתיב הקיים נשאר. **המחקר לא מצא שום דבר טוב יותר בסביבה הזאת.**

- תנועה בפילטרים של ffmpeg: `zoompan`, `crop` דינמי, glitch, פיצול RGB, `xfade`
- 🔴 **כל שוט מקודד ל-MP4 נפרד, ואז `concat`. לעולם לא רינדור אחד ארוך**
- `-preset fast -crf 20 -pix_fmt yuv420p`, 1920×1080, 30fps
- החיתוכים נופלים על הדופק. ב-150 BPM תיבה = **1.6 שניות**

**Remotion:** מפתה, אבל מרנדר דרך Chromium **פר פריים** — 2700 פריימים ל-90 שניות.
לשם השוואה, נמדד כאן: צילום שוט אחד ב-Chromium = **9.1 שניות**, ומעבר ffmpeg מלא על
90 שניות = **4.2 דקות**. 🟡 **רק לשוט טיפוגרפיה בודד (שורת המם), לא לקו הראשי.**

---

## 6. כתוביות — עברית RTL. 🔴 לא לגעת

הנתיב הזה נבדק, נכשל, אובחן ותוקן במדידה. **המחקר לא מצא סיבה לשנות בו כלום.**

- כל שורה נכתבת דרך `tools/heb_ass.py` — הוא עוטף ב-`U+202B`…`U+202C` ומבריח תווים
- 🔴 **אין להריץ `python-bidi`** — נמדד: היפוך כפול
- 🔴 **אין תגי עיצוב בתוך שורה עברית** — שוברים את סדר המילים
- 🔴 **`10%` / `TOKENS` / `BATTERY` = שכבת גרפיקה נפרדת**, אף פעם לא בתוך הכתובית
- שורות קצרות: עד 38 תווים, מקסימום 2 שורות

**שער רגרסיה לפני כל סגירת שינוי:**
```bash
python3 tools/rtl_probe.py Rubik
bash tests/smoke.sh
```

---

## 7. MCP וסקילים — לא להוסיף

**MCP:** להשאיר את חמשת השרתים שב-`.mcp.json`. **Playwright הוא היחיד שבאמת נחוץ.**
כל שרת נוסף = עוד `npx -y` שצריך להצליח בכל עליית סשן בסביבה שנמחקת בכל פעם.
שרתי ffmpeg-MCP נבדקו ונפסלו: **קריאה ישירה ל-ffmpeg גמישה יותר וניתנת לדיבוג.**

**סקילים:** אחד בלבד שווה בדיקה — **`music-to-video`** (heygen-com/hyperframes,
Apache 2.0, ~96 אלף התקנות). **לא להתקין לפני `add-skill-review`.**
סקילים של ElevenLabs / Suno / MiniMax נוגדים את אילוץ ה-$0.

---

## 8. אימות — הכלל שגובר על הכל

> **פיצ'ר או שוט אינו גמור בלי תוצר אמיתי שמוכיח שהוא עובד.**
> קוד יציאה 0 מ-ffmpeg אינו הוכחה. פריים שחור, אודיו שקט וקובץ באורך 0
> כולם מחזירים 0.

לכל תוצר:

| סוג | הבדיקה |
|---|---|
| אודיו | `ffprobe` למשך + `volumedetect`. מתחת ל-‎-40dB = שקט = כישלון |
| **מגדר הקול** | **חובה על כל אודיו עם קול** — הנוהל למטה |
| תמונה | **לפתוח ולראות בעין** — במיוחד "אין נשים" |
| וידאו | `ffprobe`: יש stream וידאו **וגם** אודיו, `pix_fmt=yuv420p` |
| כתובית עברית | `rtl_probe.py` — **לא בעין.** בדיקה חזותית של טקסט דו-כיווני אינה אמינה |

### בדיקת מגדר הקול — חובה

`tools/tts.py` חוסם מזהי-קול נשיים, **אבל הוא מכסה רק את Kokoro.** קול שמגיע
מ-Lyria או מ-Gemini TTS לא עובר דרכו כלל. לכן כל קובץ אודיו עם קול **נבדק
במדידה על האודיו עצמו**, לפני שהוא נכנס לריפו:

```bash
b64=$(base64 -w0 <קובץ האודיו>)
printf '{"contents":[{"parts":[{"inline_data":{"mime_type":"audio/mpeg","data":"%s"}},
  {"text":"Listen. Is the singing voice male or female? Answer ONLY JSON:
  {\"singer_gender\":\"male\"|\"female\"|\"unclear\",\"confidence\":\"high\"|\"medium\"|\"low\"}"}]}]}' \
  "$b64" > /tmp/voice.json

curl -s -X POST \
 "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-lite:generateContent?key=$GEMINI_API_KEY" \
 -H "Content-Type: application/json" -d @/tmp/voice.json \
 | jq -r '.candidates[0].content.parts[0].text'
```

`gemini-3.1-flash-lite` הוא הזול ביותר (במפתח Tier 1 שבסביבה הוא מחויב, אבל
בשברירי אגורה לקובץ).
**כל תשובה שאינה `male` בביטחון גבוה ⇒ הקובץ נפסל ומיוצר מחדש.**

🔑 **ולהריץ תמיד גם קובץ בקרה שידוע שהוא גברי** (למשל
`tests/research/gemini_tts_line.wav`). אם השופט טועה בבקרה — **הבדיקה פגומה,
לא התוצר**, ואסור להאמין לפסק הדין שלה.

### למה דווקא כך, ולא לפי גובה הצליל

ניסיתי קודם למדוד את תדר היסוד (F0) של השירה. התוצאה: חציון 296Hz ו-74%
מהמסגרות מעל 200Hz — כלומר "קול אישה". **הבדיקה הזאת הייתה שגויה.** היא רצה
על המיקס המלא, ומדדה סינתיסייזרים, האטס והרמוניות — לא את הקול. שלושה מודלים
עצמאיים ששמעו את האודיו עצמו קבעו **קול גברי בביטחון גבוה**, וקובץ הבקרה עבר.

> **לאמת את הבדיקה לפני שמאמינים לפסק הדין שלה.** בסשן ההקמה שלוש בדיקות מדידה
> החזירו תוצאה שגויה כי הבדיקות היו פגומות, לא התוצר. **זו הפעם הרביעית.**

---

## 9. מה השתנה מול המסמכים הקודמים

| נושא | היה | עכשיו | למה |
|---|---|---|---|
| שיר | ביט numpy + Kokoro (talk-rap) | **Lyria 3** *(B/C)* | שירה אמיתית ב-8 סנט |
| סנכרון | faster-whisper על השירה | **חותמות של Lyria** | מגיעות מוכנות — שלב שלם נמחק |
| קול | Kokoro | **Gemini TTS** | חינם רשמית, איכות טובה יותר |
| תמונות | Pollinations | **Nano Banana** *(B/C)* · **Cloudflare** *(A)* | נמדד: Pollinations החזיר 1024×576 ומודל אחר ממה שביקשתי |
| וידאו | אין | **Veo — אסור בלי אישור בכתב** | עד $54 ל-90 שניות |
| נעילת דמות | 4 seeds + בחירה | **עריכת תמונת רפרנס** | נעילה חזקה יותר |
| שוטי מלאי | לא נשקל | **Pexels/Pixabay** | תנועה אמיתית ב-$0 |
| MCP / סקילים | 5 שרתים | **בלי שינוי** | ffmpeg מקומי חזק יותר |
| עברית RTL | libass + `U+202B` | **בלי שינוי** | פתור ומאומת |
| beat.py / tts.py | הקו הראשי | **גיבוי** | לא למחוק |

---

## 10. שער QA — בלי שינוי

הרשימה החוסמת נשארת **סעיף 9 ב-`מסמכים/2. סיכום שיחה עם קלוד.md`**, בתוספת:

- ☐ **העלות בפועל בתוך התקרה שאושרה** (~$1.50, סעיף 0 כאן)
- ☐ **לא הופעל Veo**
- ☐ **הווידאו הסופי 1920×1080** — `ffprobe -show_entries stream=width,height`.
  דרישת Full HD ממסמך 1, ואף רשימה קודמת לא בדקה אותה
- ☐ **כל שוט של דמות נראה כמו צילום** ולא כמו ציור — נבדק בעין, שוט-שוט
- ☐ **הדמות זהה בין השוטים** — אותו שיער, משקפיים, הודי, זקן
- ☐ **עוצמת המאסטר בין ‎-10 ל-‎-7 LUFS** (`tools/loudness.py`), לפי `מסמכים/מחקר/2`
