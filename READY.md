# דוח מוכנות — סביבת הפקת קליפ מוזיקלי AI

**תאריך:** 13.8.2026 · **ריפו:** `hopetohelp/content-creator`

> **סביבת ההפקה: ענן (Ubuntu 24.04, 4 ליבות, 15GB, בלי GPU).**
> הנימוק המלא, כולל המדידות שהכריעו: `מסמכים/3. למה הפקה בענן.md`.

> ## ⚠️ עודכן 13.8.2026 — שתי שכבות בדוח הזה הוחלפו
>
> הדוח נכתב כשההנחה הייתה **$0 מוחלט בלי מפתחות**. הבעלים אישר מאז
> **מסלול B (~₪7)**, וזה משנה שתי שכבות:
>
> | שכבה | מה כתוב בדוח | מה מחייב היום |
> |---|---|---|
> | **תמונות ודמות** | "אין שוטים של דמות — ויזואליה פרוצדורלית בלבד" (סיכון 5) | **Nano Banana — תמונות פוטוריאליסטיות של הדמות.** נמדד ונצפה |
> | **שיר וקול** | Kokoro + `beat.py` כקו ראשי | **Lyria (שיר) + Gemini TTS (קול).** Kokoro ו-`beat.py` = גיבוי |
>
> 🔴 **דרישת בעלים חדשה: התמונות חייבות להיראות ריאליסטיות.** זה פוסל את
> Pollinations. הפרטים: `CLAUDE.md` פרק 1 · `מסמכים/6` סעיף 4.
>
> **מה בדוח הזה נשאר תקף במלואו:** נתיב ה-RTL (פרק 3), תוצאת ה-smoke test
> (פרק 5), וסיכונים 1, 1ב, 1ג, 2, 3, 4, 6, 7, 8.

---

## 1. שכבת ההפקה שנבחרה

### 🟩 שכבה C — נתיב פרוצדורלי מלא

**זו לא הייתה בחירה, זו הייתה מסקנה.** בסביבה אין GPU בכלל:

| בדיקה | תוצאה |
|---|---|
| `nvidia-smi` | לא קיים |
| `/dev/dri` | לא קיים — אין שום התקן GPU, גם לא Intel/AMD |
| `rocm-smi` | לא קיים |
| VRAM | **0** |
| CPU / RAM / דיסק | Intel Xeon 4 ליבות · 15GB · 24GB פנויים |

שכבה A דורשת 12GB VRAM ומעלה, שכבה B דורשת 6–11GB. שתיהן נפסלו על הסף.
ComfyUI לא הותקן — בלי GPU הוא רק היה מוסיף זמן, מקום וסיכון בלי שום תמורה.

---

## 2. הכלים שנבחרו, והחלופות שנפסלו

**נבחרו:** `tools/shot.js` (HTML/CSS → PNG דרך Chromium — דטרמיניסטי, אפס תלות
ברשת בזמן ההפקה, ובדיוק האסתטיקה של הקליפ) · ffmpeg 6.1.1 לתנועה (Ken Burns,
זום, whip pan, glitch, RGB split, shake) · `tools/tts.py` (Kokoro, נבדק:
3.00s, 24kHz, `mean=-18.3dB`) · `tools/beat.py` (ביט מסונתז, 150 BPM, דטרמיניסטי).

**נפסלו, כולם מאותה סיבה — אין GPU:** ComfyUI + Wan 2.2 / Flux · Stable Diffusion
מקומי (דקות ארוכות לתמונה על 4 ליבות) · ACE-Step (23 תלויות, דורש GPU בפועל) ·
YuE. **ובנוסף:** Flux דרך Hugging Face Spaces ציבורי נפסל כי הוא תלוי בשירות
חיצוני ובמכסות — הסיכון הגבוה ביותר להיתקעות בהרצה לא מפוקחת.

> **מתועד:** בנתיב הזה "שירה" היא **ראפ כדיבור קצבי מעל ביט**. אין מנוע שירה
> אמיתי שרץ על CPU. *(מאז אושר מסלול B — השירה מגיעה מ-Lyria, וזה נשאר כגיבוי.)*

---

## 3. נתיב עברית RTL

### ✅ libass מקורי + כפיית כיוון-בסיס ב-`U+202B` … `U+202C`  (לא python-bidi)

ffmpeg כאן נבנה עם `--enable-libass --enable-libfribidi --enable-libharfbuzz`,
ו-`libass.so.9` מקושר בפועל ל-`libfribidi.so.0` (אומת ב-`ldd`).

**הבדיקה החזותית הכשילה אותי, ולכן הוחלפה במדידה.** הפריים "נראה תקין", אבל
שתי בדיקות מספריות סתרו זו את זו. נבנה `tools/rtl_probe.py` שמזהה גליפים
בהשוואת פיקסלים מול רינדור-רפרנס, בלי לקרוא עברית בכלל.

**האבחנה:** libass **כן** מיישם bidi — ריצת העברית מתהפכת נכון. הכשל היה
צר ושקט: **כיוון-הבסיס של הפסקה נקבע כ-LTR**, ולכן פיסוק בקצה השורה
(`.` `!` `?`) נחת בקצה **הימני** במקום השמאלי.

| תיקון שנבדק | גליף שמאלי | גליף ימני | תוצאה |
|---|---|---|---|
| בלי כלום | `ר` | `.` | ❌ הנקודה בצד הלא נכון |
| **`U+202B` + `U+202C`** | **`.`** | **`נ`** | ✅ **נבחר** |
| `U+2067` + `U+2069` | `.` | `נ` | ✅ עובד גם |
| `U+200F` (RLM) | `ר` | `.` | ❌ לא משנה כיוון-בסיס |
| **python-bidi** `get_display()` | `.` | `ר` | ❌ **מזיק** — היפוך כפול |

> 🔴 **python-bidi נפסל בראיה מדודה.** הוא מיועד לרנדרר שאין לו bidi. כאן יש,
> ולכן הוא הופך פעם שנייה והעברית נקראת הפוך.
> **הוסר לגמרי מ-`bootstrap.sh` ב-13.8.2026** — אין טעם להתקין ספרייה אסורה.

כל שורת כתובית נכתבת דרך `tools/heb_ass.py`, שעוטף אוטומטית ומבריח תווים.

---

## 4. שרתי MCP — 5/5 מחוברים

חמשת השרתים מוגדרים ב-`.mcp.json` שבריפו: `memory` · `seq` · `fetch` ·
`context7` · `playwright`. ‏`comfy-mcp` לא הותקן (אין ComfyUI; הקהילתי
`artokun/comfyui-mcp` לא נשקל בגלל ממצא אבטחה).

**שלושה דברים שנלמדו בדרך הקשה, וכולם כבר צרובים בקבצים:**
1. **`fetch` נפל** על `ImportError: McpError` — החבילה מצהירה `mcp>=1.1.3` ו-uv
   פתר ל-`mcp 2.0.0` שבו הסמל הוזז. התיקון (`--with "mcp<2"`) יושב ב-`.mcp.json`.
2. **הגדרה ב-`--scope project`** ⇒ `.mcp.json` בתוך הריפו. ברירת המחדל כותבת
   ל-`/root/.claude.json`, והקונטיינר החד-פעמי היה מוחק את כולם.
3. **`enableAllProjectMcpServers`** ב-`.claude/settings.json` — בלעדיו השרתים
   עולים כ-"Pending approval", שער אינטראקטיבי שעוצר סשן לא-מפוקח.

---

## 5. תוצאת ה-Smoke Test

**`tests/smoke.mp4` — ✅ עבר.** ניתן להרצה חוזרת: `bash tests/smoke.sh`

| דרישה | נמדד |
|---|---|
| משך ~5 שניות | `5.00s` ✅ |
| stream וידאו | h264, 1920x1080, 125 פריימים ✅ |
| stream אודיו | aac, 44100Hz, סטריאו ✅ |
| `pix_fmt=yuv420p` | ✅ |
| אודיו מייצור אמיתי | Kokoro 3s + ביט 5s, `mean=-23.4dB` (לא שקט) ✅ |
| כתובית עברית צרובה | הגליף השמאלי 7x7px על קו הבסיס = נקודה ✅ |
| שכבת גרפיקה נפרדת `10%` | `%` (118px) בקצה הימני ⇒ `10%` ולא `%01` ✅ |

הפריים `tests/smoke_frame.png` (t=2.5s) **נצפה בפועל** ואומת גם במדידה.

> **הערה מתודולוגית שחוזרת פעמיים בדוח הזה:** גם באימות ה-smoke שתי הבדיקות
> הראשונות החזירו ❌ — ושתיהן היו פגומות, לא התוצר. אחת מיזגה את הנקודה לתוך
> המילה; השנייה הניחה ש-`1` צר מ-`0` בפונט **מונוספייס**. **לאמת את הבדיקה
> לפני שמאמינים לפסק הדין שלה.**

---

## 6. סיכונים בהרצה האמיתית, וחלופה לכל אחד

| # | סיכון | סבירות | חלופה |
|---|---|---|---|
| 1 | **הקונטיינר חד-פעמי** — ffmpeg, torch, Kokoro, פונטים (~6GB) ייעלמו | **ודאי** | `bash tools/bootstrap.sh` — משחזר הכל, אידמפוטנטי. להריץ ראשון בכל סשן; `session-start.sh` מתריע אוטומטית אם חסר. |
| 1ב | **סשן נקטע באמצע הפקה** ⇒ אובדן תוצרים | בינונית | שלושה מנגנונים: `.gitignore` שומר תוצרים, `tools/checkpoint.sh` אחרי כל שלב, ורינדור בקטעים ל-`output/segments/`. סשן שנקטע ממשיך מהקטע הבא. |
| 1ג | **שני סשנים עובדים על אותו שלב** | בינונית | `tools/claim.sh` תופס שלב ודוחף ל-main לפני העבודה הראשונה, ומסרב לתפוס שלב שכבר תפוס. `PROGRESS.md` הוא מקור האמת. |
| 2 | **משקלי Kokoro (~330MB) יורדים מ-HF בכל סשן**, בלי טוקן ⇒ מכסת קצב | בינונית | להגדיר `HF_TOKEN`; ואם נופל — ביט בלבד + טקסט על המסך. הקליפ שורד בלי קול, לא בלי ביט. |
| 3 | **זמן רינדור על 4 ליבות** — 90 שניות ב-1080p עם פילטרים כבדים | בינונית-גבוהה | לרנדר בקטעים ולשרשר (`concat`); `-preset faster`; פילטרים יקרים רק בשוטים שצריכים אותם. |
| 4 | **גלישת שורה בעברית** — שורה ארוכה שנשברת לשתיים לא נבדקה, וכיוון-הבסיס עלול להתנהג אחרת בשורה השנייה | בינונית | לשמור שורות קצרות (≤42 תווים) ולשבור ידנית ב-`\N`; להריץ `rtl_probe` על שורה שבורה לפני שימוש. |
| 5 | ~~**אין שוטים של דמות**~~ — **בוטל 13.8.2026** | — | הסיכון נסגר: **Nano Banana** מייצר את הדמות פוטוריאליסטית דרך API, בלי GPU מקומי. ⚠️ נלווית אליו חובה שלא הייתה קודם: **כל תמונה נבדקת בעין** מול הכלל "אין נשים" — הוא כבר לא מובטח מאליו. |
| 6 | **רגרסיה ב-bidi** אם ffmpeg/libass ישתנה | נמוכה | `python3 tools/rtl_probe.py Rubik` הוא שער חוסם — הוא כבר בתוך `bootstrap.sh`. |
| 7 | **`npx -y` של שרתי MCP דורש רשת** בכל עלייה | נמוכה | לא חוסם הפקה — הכלים המרכזיים (ffmpeg/python/node) מקומיים. |
| 8 | **דיסק** — 24GB פנויים, torch+מודלים כבר תפסו ~6GB | נמוכה | `pip cache purge` (שחרר 2.8GB בסשן הזה); שוטים ב-1x במקום 2x. |

---

## 7. הכרעה

# ✅ GO

**הנימוק:** כל חוליה בשרשרת לא רק הותקנה אלא **הופעלה והוכחה על תוצר בפועל** —
`tests/smoke.mp4` מכיל וידאו, אודיו מייצור אמיתי, כתובית עברית תקינה ושכבת
גרפיקה נפרדת, ואומת גם ב-`ffprobe`, גם בעין וגם במדידת פיקסלים. הסיכון
המסוכן ביותר — עברית RTL — נסגר עם אבחנה מדויקת, תיקון מאומת, וכלי רגרסיה.
כל שאר הסיכונים הם ביצועים או תזמון, ולכולם יש חלופה כתובה. אין בלוקר פתוח.

**התנאים:** בכל סשן חדש להריץ `bash tools/bootstrap.sh` לפני כל דבר אחר,
לתפוס שלב ב-`claim.sh` לפני העבודה הראשונה, ולעשות צ'קפוינט אחרי כל שלב.
