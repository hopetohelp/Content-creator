# דוח מוכנות — סביבת הפקת קליפ מוזיקלי AI

**תאריך:** 13.8.2026 · **ריפו:** `hopetohelp/content-creator` · **ענף:** `claude/ai-music-clip-setup-y2mq6s`
**יומן מלא של כל צעד:** `setup.log` (225 שורות, חותמות זמן UTC)

> ### ⚠️ קראו קודם — פער סביבה
> ההקמה בוצעה ואומתה על **Ubuntu 24.04 בענן** (Xeon 4 ליבות, 15GB RAM, בלי GPU).
> הנחיית הפרויקט ב-`מסמכים/` מתארת מכונה אחרת: **HP 14s, Windows, i3-1115G4,
> 8GB RAM, Intel UHD**. הדוח הזה תקף לסביבה שבה הוא נבדק בפועל.
>
> **מה תקף בשתי הסביבות:** כל ההכרעות המהותיות — שכבה C, פסילת ACE-Step/YuE,
> נתיב ה-RTL, בחירת Kokoro, והכלים עצמם (פייתון/Node/ffmpeg חוצי-פלטפורמה).
> שתי המכונות חסרות GPU שימושי, ולכן המסקנה זהה.
>
> **מה לא תקף:** `tools/bootstrap.sh` כתוב ל-apt. על Windows יידרשו
> `winget install Gyan.FFmpeg`, התקנת פונטים ידנית, ו-`espeak-ng` נפרד.
> ראו סיכון #0 בטבלה.

---

## 1. שכבת ההפקה שנבחרה

### 🟩 שכבה C — נתיב פרוצדורלי מלא

**זו לא הייתה בחירה, זו הייתה מסקנה.** בסביבה אין GPU בכלל:

| בדיקה | תוצאה |
|---|---|
| `nvidia-smi` | לא קיים |
| `/dev/dri` | לא קיים — אין שום התקן GPU, גם לא Intel/AMD |
| `rocm-smi` | לא קיים |
| VRAM | **0** |
| CPU / RAM / דיסק | Intel Xeon 4 ליבות · 15GB · 24GB פנויים |

שכבה A דורשת 12GB VRAM ומעלה, שכבה B דורשת 6–11GB. שתיהן נפסלו על הסף.
ComfyUI לא הותקן — בלי GPU הוא רק היה מוסיף זמן, מקום וסיכון בלי שום תמורה.

---

## 2. הכלים שנבחרו, והחלופות שנפסלו

### וידאו — Chromium (Playwright) + ffmpeg

| | הכלי | למה |
|---|---|---|
| ✅ **נבחר** | `tools/shot.js` — HTML/CSS → PNG דרך Chromium | דטרמיניסטי, אפס תלות ברשת בזמן ההפקה, ובדיוק האסתטיקה של הקליפ (טרמינל/IDE/טלפון/התראות). Chromium כבר מותקן ב-`/opt/pw-browsers`. |
| ✅ **נבחר** | ffmpeg 6.1.1 לתנועה | Ken Burns, זום, whip pan, glitch, RGB split, shake — הכל בפילטרים. |
| ❌ נפסל | ComfyUI + Wan 2.2 / Flux (שכבות A/B) | דורש VRAM. אין. |
| ❌ נפסל | Flux דרך Hugging Face Spaces ציבורי | תלוי בשירות חיצוני, במכסות ובזמינות — הסיכון הגבוה ביותר להיתקעות בהרצה לא מפוקחת. |
| ❌ נפסל | Stable Diffusion מקומי על CPU | דקות ארוכות לתמונה על 4 ליבות. לא סביר לעשרות שוטים. |

### אודיו — Kokoro TTS + ביט פרוצדורלי

| | הכלי | למה |
|---|---|---|
| ✅ **נבחר** | `tools/tts.py` — Kokoro TTS 82M, קול `am_michael` | רץ על CPU, Apache 2.0, בלי מפתח ובלי מכסה. נבדק: 3.00s, 24kHz, `mean=-18.3dB`. |
| ✅ **נבחר** | `tools/beat.py` — ביט מסונתז ב-numpy | Kokoro נותן קול, לא ביט. 808 עם גלישת תדר, סנר, האטס, סאב. 150 BPM. דטרמיניסטי לחלוטין, אפס תלות. |
| ❌ נפסל | ACE-Step (עדיפות 1) | מודל דיפוזיה. 23 תלויות: torch, diffusers, transformers, spacy, gradio. דורש GPU בפועל. |
| ❌ נפסל | YuE (עדיפות 2) | המשימה עצמה מגבילה אותו לשכבה A. |

> **סטייה מהמשימה, מתועדת:** "שירה" בפועל תהיה **ראפ כדיבור קצבי מעל ביט** — בדיוק
> מה שאפשרות 3 במשימה מגדירה. אין מנוע שירה אמיתי שרץ על CPU בתקציב $0.

---

## 3. נתיב עברית RTL

### ✅ libass מקורי + כפיית כיוון-בסיס ב-`U+202B` … `U+202C`  (לא python-bidi)

ffmpeg כאן נבנה עם `--enable-libass --enable-libfribidi --enable-libharfbuzz`,
ו-`libass.so.9` מקושר בפועל ל-`libfribidi.so.0` (אומת ב-`ldd`).

**הבדיקה החזותית הכשילה אותי, ולכן הוחלפה במדידה.** הפריים "נראה תקין", אבל
שתי בדיקות מספריות סתרו זו את זו. נבנה `tools/rtl_probe.py` שמזהה גליפים
בהשוואת פיקסלים מול רינדור-רפרנס, בלי לקרוא עברית בכלל.

**האבחנה:** libass **כן** מיישם bidi — ריצת העברית מתהפכת נכון. הכשל היה
צר ושקט: **כיוון-הבסיס של הפסקה נקבע כ-LTR**, ולכן פיסוק בקצה השורה
(`.` `!` `?`) נחת בקצה **הימני** במקום השמאלי.

| תיקון שנבדק | גליף שמאלי | גליף ימני | תוצאה |
|---|---|---|---|
| בלי כלום | `ר` | `.` | ❌ הנקודה בצד הלא נכון |
| **`U+202B` + `U+202C`** | **`.`** | **`נ`** | ✅ **נבחר** |
| `U+2067` + `U+2069` | `.` | `נ` | ✅ עובד גם |
| `U+200F` (RLM) | `ר` | `.` | ❌ לא משנה כיוון-בסיס |
| **python-bidi** `get_display()` | `.` | `ר` | ❌ **מזיק** — היפוך כפול |

> 🔴 **python-bidi נפסל בראיה מדודה.** הוא מיועד לרנדרר שאין לו bidi. כאן יש,
> ולכן הוא הופך פעם שנייה והעברית נקראת הפוך. הוא מותקן (המשימה ביקשה) אבל
> **אין להשתמש בו** — התיעוד ב-`CLAUDE.md` וב-`tools/heb_ass.py`.

כל שורת כתובית נכתבת דרך `tools/heb_ass.py`, שעוטף אוטומטית ומבריח תווים.

---

## 4. שרתי MCP — 5/5 מחוברים

| שרת | סטטוס |
|---|---|
| `memory` | ✅ Connected |
| `seq` (sequential-thinking) | ✅ Connected |
| `fetch` | ✅ Connected — **אחרי תיקון** |
| `context7` | ✅ Connected |
| `playwright` | ✅ Connected |
| `comfy-mcp` | לא הותקן — אין ComfyUI. הקהילתי `artokun/comfyui-mcp` לא נשקל (ממצא אבטחה). |

**`fetch` נפל ותוקן:** `ImportError: cannot import name 'McpError'`. השורש במעלה
הזרם — החבילה מצהירה `mcp>=1.1.3` ו-uv פתר ל-`mcp 2.0.0` שבו הסמל הוזז.
התיקון: `uvx --with "mcp<2" --from mcp-server-fetch mcp-server-fetch`.

**שני שיפורי עמידות שלא נתבקשו אבל היו הכרחיים:**
1. השרתים הוגדרו ב-`--scope project` → `.mcp.json` **בתוך הריפו**. ברירת המחדל
   כותבת ל-`/root/.claude.json` המקומי, והקונטיינר החד-פעמי היה מוחק את כולם.
2. שרתי `.mcp.json` עולים כ-"Pending approval" — שער אינטראקטיבי שהיה עוצר
   בדיוק את הסשן הלא-מפוקח. נוטרל ב-`enableAllProjectMcpServers`.

---

## 5. תוצאת ה-Smoke Test

**`tests/smoke.mp4` — ✅ עבר.** ניתן להרצה חוזרת: `bash tests/smoke.sh`

| דרישה | נמדד |
|---|---|
| משך ~5 שניות | `5.00s` ✅ |
| stream וידאו | h264, 1920x1080, 125 פריימים ✅ |
| stream אודיו | aac, 44100Hz, סטריאו ✅ |
| `pix_fmt=yuv420p` | ✅ |
| אודיו מייצור אמיתי | Kokoro 3s + ביט 5s, `mean=-23.4dB` (לא שקט) ✅ |
| כתובית עברית צרובה | הגליף השמאלי 7x7px על קו הבסיס = נקודה ✅ |
| שכבת גרפיקה נפרדת `10%` | `%` (118px) בקצה הימני ⇒ `10%` ולא `%01` ✅ |

הפריים `tests/smoke_frame.png` (t=2.5s) **נצפה בפועל** ואומת גם במדידה.

> **הערה מתודולוגית שחוזרת פעמיים בדוח הזה:** גם באימות ה-smoke שתי הבדיקות
> הראשונות החזירו ❌ — ושתיהן היו פגומות, לא התוצר. אחת מיזגה את הנקודה לתוך
> המילה; השנייה הניחה ש-`1` צר מ-`0` בפונט **מונוספייס**. **לאמת את הבדיקה
> לפני שמאמינים לפסק הדין שלה.**

---

## 6. סיכונים בהרצה האמיתית, וחלופה לכל אחד

| # | סיכון | סבירות | חלופה |
|---|---|---|---|
| **0** | **הרצת ההפקה על Windows במקום Linux** — `bootstrap.sh` מבוסס apt ולא ירוץ; נתיבי הפונטים והתקנת espeak-ng שונים | **גבוהה** (ההנחיה המקורית מתארת מכונת Windows) | להריץ את ההפקה באותה סביבת Linux שבה נבנתה ואומתה. אם חייבים Windows: `winget install Gyan.FFmpeg`, להתקין פונטים בלחיצה ימנית → Install, `pip install kokoro` זהה, ולהריץ `tools/rtl_probe.py` כשער — libass ב-Windows עלול להתנהג אחרת בכיוון-הבסיס. |
| 1 | **הקונטיינר חד-פעמי** — ffmpeg, torch, Kokoro, פונטים (~6GB) ייעלמו | **ודאי** | `bash tools/bootstrap.sh` — משחזר הכל, אידמפוטנטי, כולל שתי העקיפות שנדרשו בפועל. להריץ ראשון בכל סשן. |
| 2 | **משקלי Kokoro (~330MB) יורדים מ-HF בכל סשן**, בלי טוקן ⇒ מכסת קצב | בינונית | להגדיר `HF_TOKEN`; ואם נופל — ביט בלבד + טקסט על המסך. הקליפ שורד בלי קול, לא בלי ביט. |
| 3 | **זמן רינדור על 4 ליבות** — 90 שניות ב-1080p עם פילטרים כבדים | בינונית-גבוהה | לרנדר בקטעים ולשרשר (`concat`); `-preset faster`; פילטרים יקרים רק בשוטים שצריכים אותם. |
| 4 | **גלישת שורה בעברית** — שורה ארוכה שנשברת לשתיים לא נבדקה, וכיוון-הבסיס עלול להתנהג אחרת בשורה השנייה | בינונית | לשמור שורות קצרות (≤42 תווים) ולשבור ידנית ב-`\N`; להריץ `rtl_probe` על שורה שבורה לפני שימוש. |
| 5 | **אין שוטים של דמות** — בלי GPU אין Flux/SD מעשי | ודאי | ויזואליות פרוצדורלית בלבד: מסכים, טיפוגרפיה, צלליות, הפשטה. **בונוס:** האילוץ "אין נשים בשום פריים" מובטח טריוויאלית כשאין מודל תמונות. |
| 6 | **רגרסיה ב-bidi** אם ffmpeg/libass ישתנה | נמוכה | `python3 tools/rtl_probe.py Rubik` הוא שער חוסם — הוא כבר בתוך `bootstrap.sh`. |
| 7 | **`npx -y` של שרתי MCP דורש רשת** בכל עלייה | נמוכה | לא חוסם הפקה — הכלים המרכזיים (ffmpeg/python/node) מקומיים. |
| 8 | **דיסק** — 24GB פנויים, torch+מודלים כבר תפסו ~6GB | נמוכה | `pip cache purge` (שחרר 2.8GB בסשן הזה); שוטים ב-1x במקום 2x. |

---

## 7. הכרעה

# ✅ GO

**הנימוק:** כל חוליה בשרשרת לא רק הותקנה אלא **הופעלה והוכחה על תוצר בפועל** —
`tests/smoke.mp4` מכיל וידאו, אודיו מייצור אמיתי, כתובית עברית תקינה ושכבת
גרפיקה נפרדת, ואומת גם ב-`ffprobe`, גם בעין וגם במדידת פיקסלים. הסיכון
המסוכן ביותר — עברית RTL — נסגר עם אבחנה מדויקת, תיקון מאומת, וכלי רגרסיה.
כל שאר הסיכונים הם ביצועים או תזמון, ולכולם יש חלופה כתובה. אין בלוקר פתוח.

**התנאי היחיד:** בכל סשן חדש להריץ `bash tools/bootstrap.sh` **לפני** כל דבר אחר.
